import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#C89B3C',
    },
    secondary: {
      main: '#005A82',
    },
    background: {
      main: '#010A13'
    },
    mode: 'dark'
  },
});